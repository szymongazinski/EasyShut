using System.Drawing;

namespace EasyShut
{
    internal static class AppIcon
    {
        // Keep all ICO sizes available for Windows Forms and display scaling.
        public static readonly Icon Value = new Icon(typeof(AppIcon), "App.ico");
    }
}
